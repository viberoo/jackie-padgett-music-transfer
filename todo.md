# GitHub Export Checklist

- [x] Confirm the connected GitHub account and available repository destination: `viberoo`.
- [x] Preserve the current live site and custom-domain configuration without changes.
- [x] Confirm the isolated repository strategy: do not modify `viberoo/jackie-padgett-music`.
- [x] Create the new public repository `viberoo/jackie-padgett-music-transfer` without initializing it with source files.
- [ ] Verify that the authorized GitHub identity is `viberoo` and retains access to `viberoo/jackie-padgett-music-transfer`.
- [ ] Verify the renewed `viberoo` GitHub authorization against the isolated repository.
- [ ] Create an isolated local export checkout so the managed project’s internal checkpoint remote remains unchanged.
- [ ] Push the completed checkpointed source to the new repository from the isolated checkout only.
- [ ] Confirm the export result and leave domain cutover as a separate, verified step.
